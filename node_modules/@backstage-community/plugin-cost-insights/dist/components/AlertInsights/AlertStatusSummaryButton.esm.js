import React, { useState } from 'react';
import classnames from 'classnames';
import Button from '@material-ui/core/Button';
import ExpandMoreIcon from '@material-ui/icons/ExpandMore';
import { useAlertStatusSummaryButtonStyles } from '../../utils/styles.esm.js';

const AlertStatusSummaryButton = ({
  children,
  onClick
}) => {
  const classes = useAlertStatusSummaryButtonStyles();
  const [clicked, setClicked] = useState(false);
  const iconClassName = classnames(classes.icon, {
    [classes.clicked]: clicked
  });
  const handleOnClick = (e) => {
    setClicked((prevClicked) => !prevClicked);
    onClick(e);
  };
  return /* @__PURE__ */ React.createElement(
    Button,
    {
      variant: "text",
      color: "primary",
      disableElevation: true,
      "aria-label": "expand",
      endIcon: /* @__PURE__ */ React.createElement(ExpandMoreIcon, { className: iconClassName }),
      onClick: handleOnClick
    },
    children
  );
};

export { AlertStatusSummaryButton };
//# sourceMappingURL=AlertStatusSummaryButton.esm.js.map
