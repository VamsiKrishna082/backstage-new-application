import React from 'react';
import { ThemeProvider, createTheme } from '@material-ui/core/styles';
import { costInsightsDarkTheme, costInsightsLightTheme } from '../../utils/styles.esm.js';

const CostInsightsThemeProvider = ({
  children
}) => {
  return /* @__PURE__ */ React.createElement(
    ThemeProvider,
    {
      theme: (theme) => createTheme({
        ...theme,
        palette: {
          ...theme.palette,
          ...theme.palette.type === "dark" ? costInsightsDarkTheme.palette : costInsightsLightTheme.palette
        }
      })
    },
    children
  );
};

export { CostInsightsThemeProvider };
//# sourceMappingURL=CostInsightsThemeProvider.esm.js.map
