import { convertLegacyRouteRefs } from '@backstage/core-compat-api';
import { createFrontendPlugin } from '@backstage/frontend-plugin-api';
import { entityArgoCDOverviewCard, entityArgoCDHistoryCard } from './entityCards.esm.js';
import { argoCDApiExtension } from './apis.esm.js';
import { entityContentRouteRef } from '../plugin.esm.js';
import { argoCdPage } from './pages.esm.js';

const plugin = createFrontendPlugin({
  id: "argocd",
  extensions: [
    argoCdPage,
    entityArgoCDOverviewCard,
    entityArgoCDHistoryCard,
    argoCDApiExtension
  ],
  routes: convertLegacyRouteRefs({ argocd: entityContentRouteRef })
});

export { plugin as default };
//# sourceMappingURL=plugin.esm.js.map
